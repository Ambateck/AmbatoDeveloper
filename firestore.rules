rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Permite leer a cualquiera, pero escribir solo a usuarios autenticados.
    match /{document=**} {
      allow read;
      allow write: if request.auth != null;
    }
  }
}
